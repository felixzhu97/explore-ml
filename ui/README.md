# ui

Placeholder for a future model-test front end that talks to services under
`../python_ml/` (loopback). Not implemented in the extract cut.

Keep this directory sibling to `python_ml/` — do not nest UI under the API tree.
